# Chaos Camp Profile

Personal profile page for 東大カオスキャンプ.

## Preview

Open `index.html` directly, or run the tiny local server:

```powershell
& "C:\Users\TaoTa\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe" .\server.mjs
```

Then visit `http://localhost:4173`.

## Replace Content

- Replace `public/images/portrait.jpg` with a real portrait when available.
- Contact email is set to `zhangzhuoquntaku@gmail.com`.
- Project images are extracted from `01 _ Profile・About Me.pdf`.
